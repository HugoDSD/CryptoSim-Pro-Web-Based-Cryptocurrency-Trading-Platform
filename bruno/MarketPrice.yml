info:
  name: MarketPrice
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5025/api/market/marketprices
  body:
    type: json
    data: |-
      {
        "email": "ILONA.test@efrei.fr",
        "password": "Password123!",
        "name": "Hugo",
        "surname": "DE SOUSA DIAS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
