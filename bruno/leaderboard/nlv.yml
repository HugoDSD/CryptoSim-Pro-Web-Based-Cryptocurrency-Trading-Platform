info:
  name: nlv
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:<ton_port>/api/Trading/leaderboard?sortBy=activity
  params:
    - name: sortBy
      value: activity
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
