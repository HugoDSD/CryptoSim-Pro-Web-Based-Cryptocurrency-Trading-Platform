info:
  name: crypto value
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:<ton_port>/api/Trading/leaderboard?sortBy=nlv
  params:
    - name: sortBy
      value: nlv
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
