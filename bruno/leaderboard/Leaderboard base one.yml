info:
  name: Leaderboard base one
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:<ton_port>/api/Trading/leaderboard
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
