info:
  name: Pourcentage de gain
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:<ton_port>/api/Trading/leaderboard?sortBy=percentage
  params:
    - name: sortBy
      value: percentage
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
